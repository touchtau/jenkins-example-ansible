pipeline {
  agent {label 'dev'}

  stages {
    stage('Hello') {
      steps {
        sh '''
          sudo ansible centos -m ping
          ansible --version
          ansible-playbook --version
          ansible-galaxy --version
        '''
      }
    }
  }
}
